export enum StatusEnum {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  PENDING = 'pending',
  BLOCKED = 'blocked',
}

export enum Order {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum SortEnum {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum UserRoleEnum {
  ADMIN = 'Admin',
  USER = 'User',
  SUPER_ADMIN = 'Super Admin',
}
